name: Keep Alive2
on:
  schedule:
    - cron: '*/5 * * * *'  # Запросы каждые 5 минут
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Server
        run: curl -s https://TunkaSQDbot.rotveller2012.repl.co/ping
